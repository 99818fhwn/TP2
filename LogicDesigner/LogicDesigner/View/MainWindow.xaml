<Window x:Class="LogicDesigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogicDesigner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                            BorderThickness="1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter Margin="2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Name="ComponentGrid">
        <Grid Name="SampleComponent" Height="Auto" Width="Auto">
            <Button
            PreviewMouseDown="ComponentMouseDown"
            PreviewMouseUp="ComponentMouseUp"
            PreviewMouseMove="ComponentMouseMove"
            Width="50" Height="50">
                <Button.Background>
                    <ImageBrush ImageSource="/Images/And.jpg" Stretch="Fill"/>
                </Button.Background>
            </Button>
            <TextBlock Height="30" Width="20" Margin = "0 0 0 100" Background="Red"/>
        </Grid>        
    </Grid>
</Window>
